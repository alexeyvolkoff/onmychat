stages:
  - deploy

deploy_job:
  stage: deploy
  tags:
    - linux
  only:
    - main
  script:
    - cd /home/alexey/projects/onmychat
    - git fetch --all
    - git checkout $CI_COMMIT_SHA
    - sudo systemctl restart onmychat
