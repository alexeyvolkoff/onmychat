*This is not a conversational request, simply create an image prompt*.
Return 'Title: short title' on the first line (3-4 words max), then 'Image: prompt' on the next line.

Create a high-quality SDXL prompt in ComfyUI / Danbooru tag style based on the conversation history and the requested object or scene: {prompt}

**CRITICAL — Follow ALL requirements:**

**1. SCENE CONTEXT (from conversation history) — REQUIRED:**
Analyze the RECENT CONVERSATION HISTORY above. Extract and include the environment, lighting, and current situation. If the scene involves the assistant/companion character, you MUST incorporate their presence.

WARNING: Do NOT generate a generic image. The image MUST reflect the specific scene, location, and details from the conversation history.

**2. CHARACTER APPEARANCE — if character is in the scene:**
If the conversation context or the requested scene involves the companion character, you MUST strictly use their EXACT physical appearance. Do NOT use default or generic descriptions:
{appearance}

**3. STYLE & FORMAT (Danbooru Tag Style):**
   — Comma-separated tags only, lowercase, no full sentences
   — Structure: `[Main Subject], [Details/Pose], [Environment/Location], [Lighting], [Style/Quality Tags]`
   — Perspective: External observer view
   — Use parentheses for emphasis: `(keyword:1.1)` or `((keyword))`
   — Avoid 'face covered in <>' — use '<>-covered face' instead

**4. OUTPUT RULES:**
   — ONLY output TWO lines: `Title:` line first, then `Image:` line
   — Title: 3-4 words max
   — NO full sentences, NO explanations, NO markdown, NO lists

**Example:**
Title: Rainy Night Balcony
Image: 1girl, solo, leaning on balcony railing, (intricate black lace dress:1.2), long auburn hair flowing in wind, night cityscape background, neon lights reflection, pensive expression, cinematic lighting, wet pavement, moody atmosphere, photorealistic

**Result:**
Title: [Title]
Image: [Prompt string]